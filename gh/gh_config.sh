#!/bin/bash

# Show link to github.com for a file in current branch using: gh url FILE
# Supports --branch BRANCH_NAME argument. Defaults to current branch.
gh alias set url 'browse --no-browser'

echo 'All done!'

